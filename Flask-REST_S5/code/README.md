To run this app, it is recommended to install python virtualenv and create a virtual evironment with version 3.6

Then run the activate-script to start the virtual env and install the required modules.